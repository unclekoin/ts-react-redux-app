import React, {FC} from 'react';
import UserList from './components/user-list';

const App: FC = () => {
    return (
        <div>
          <UserList />
        </div>
    );
};

export default App;
